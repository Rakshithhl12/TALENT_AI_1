# Core
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.18.0
openpyxl>=3.1.2

# SQLAlchemy ORM — works with any database
sqlalchemy>=2.0.0

# Database drivers — install the one you need:
pymysql>=1.1.0          # MySQL:      mysql+pymysql://
psycopg2-binary>=2.9.0  # PostgreSQL/Supabase/Neon: postgresql+psycopg2://
# sqlite3 is built into Python — no install needed for sqlite://

# Resume parsing
pdfplumber>=0.10.0
python-docx>=1.1.0
